# Daily report — 2026-09-08

Orphan-state check: session's own git clone was SHALLOW again (grafted at
357150b/9696f68 — the same false-truncation pattern first diagnosed 2026-09-04).
`git fetch --unshallow` confirmed `main`'s true root is still the unbroken
2026-06-26 scaffold (84 commits) and re-confirmed the two stray branches'
relevant tips are BYTE-IDENTICAL duplicates of content `main` already carries
(`git diff` empty) — no merge needed, consistent with every session since
2026-08-24. `wizardly-turing-skvl3m` remains a plain ancestor of `main`.

Quiet run: arXiv quant-ph (+ QML/cs.ET cross-lists) posted **zero** new items —
confirmed live via the API (newest entry still Sep-04T17:56:24Z, unchanged from
yesterday's cutoff). The US Labor Day holiday (Sep-7) extended the usual
weekend posting freeze. No stage moves, no cap-swaps.

## Ledger changes

- None on the trend table. **1 new observation_queue item**: Quantum Machines'
  ["What a Non-Clifford Circuit Taught Us About Fault Tolerant Control"](https://www.quantum-machines.co/resources/blog/non-clifford-fault-tolerant-control)
  — a real-time QEC control account (decoding-dependent feed-forward for
  non-Clifford gates, derived from a small Shor's-algorithm circuit). Undated
  on the page, so queued unverified rather than promoted to evidence.
- **Partial source heal**: this is the first run since Quantum Machines' 2026-09-05
  promotion where `tvly extract` returned real article content (both the blog
  listing and a direct post URL) instead of a cookie-consent/nav shell. Treated
  as a 1-data-point heal — re-verify next run before fully trusting it.

## Top 3 of the day

1. [Quantum Machines: real-time QEC control for non-Clifford gates](https://www.quantum-machines.co/resources/blog/non-clifford-fault-tolerant-control) — only genuinely new item this run; queued (undated, unverified).
2. Confirmed arXiv is fully quiet since the Sep-04 cutoff — a real gap (Labor Day), not a missed check.
3. Resolved the recurring shallow-clone false alarm on the two stray branches again — byte-identical to `main`, no action needed.

## Study picks

None today — the only new capture is undated/unverified (below the study_shelf bar per the standing Quantum Machines source caveat).

## Queue (18 → 19)

1 new item (Quantum Machines, above). 0 dropped — the oldest live item is
2026-09-01 (7 days), not stale. Two strong flagged cap-swap candidates from
the 2026-09-07 run remain held and unresolved for a 2nd daily running:
trend-004 (representation-theoretic barren-plateau framework, 2609.04462) and
trend-003 (SAR/InSAR quantum generative models on real IonQ hardware,
2609.05313) — flag for next run to avoid letting them sit past a 3rd.

Checked-but-excluded: a 404media.co "cosmic mystery" piece (paywalled
aggregator, not a primary); a HN "IBM_phoenix" link (resolves to IBM's live
system-status page for the already-captured Nighthawk r2 QPU, no new
information); a Reddit r/science charge-noise repost tracing to a stale
Jun-5-2026 item.

**capture-leak: 14 ids checked against the Sep-07 report / 0 missing.**

## Next

- Resolve the trend-004 and trend-003 flagged cap-swap candidates next run
  rather than let them sit past a 3rd daily.
- Re-verify the Quantum Machines extraction heal on the next 1-2 runs; if it
  holds, also try to recover a publish date so the item can move off
  "undated, unverified."
- IBM+Qedma Floquet claim (2607.24937) — still watching, no development this run.
- npj QI RSS returned no items this run (channel metadata only) — 1st
  occurrence, not yet a forced-heal trigger; re-check next run.
